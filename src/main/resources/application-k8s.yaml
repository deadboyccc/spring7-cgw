spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://auth-server:9000
  cloud:
    gateway:
      routes:
        - id: spring-6-rest-mvc
          uri: http://rest-mvc:8080
          predicates:
            - Path=/api/v1/*/**
        - id: spring-6-reactive
          uri: http://reactive:8080
          predicates:
            - Path=/api/v2/**
        - id: spring-6-auth-server
          uri: http://auth-server:9090
          predicates:
            - Path=/oauth2/**
          filters:
            - name: SetRequestHostHeader
              args:
                host: auth-server